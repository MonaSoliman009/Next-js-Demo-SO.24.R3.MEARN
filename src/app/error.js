"use client"
import React from 'react';

const Error = ({error}) => {

    console.log(error);
    
    return (
        <div>
            This is Error page
        </div>
    );
}

export default Error;
